package notUsed;

import java.util.ArrayList;
//"Это класс для игры в одномерном массиве с одним сайтом
public class DotComTestDrive {
    /*public static void main(String[] args) {
        int numOfGuesses = 0;
        GameHelper helper = new GameHelper();
        DotCom theDotCom1 = new DotCom();
        int randomNumx = (int) (Math.random() * 5);
        String randomNum1 = Integer.toString(randomNumx+0);
        String randomNum2 = Integer.toString(randomNumx+1);
        String randomNum3 = Integer.toString(randomNumx+2);
        ArrayList<String> locations = new ArrayList<String>();
        locations.add(randomNum1);
        locations.add(randomNum2);
        locations.add(randomNum3);
        theDotCom1.setLocationcells(locations);
        boolean isAlive = true;
        while (isAlive == true) {
            String guess = helper.getUserInput("Введите число:\n");
            String result = theDotCom1.checkYourself(guess);
            numOfGuesses++;
            if (result.equals("Sank")) {
                isAlive = false;
                System.out.println("Вам потребовалось " + numOfGuesses + " попыток(и)");
            }
        }
    }*/
}